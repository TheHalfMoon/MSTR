# Implementation Handoff — MSTR-000B

## Objective

Implement the strongest evidence-driven pre-training foundation for MSTR's code-specialized mission without starting training or weakening the universal-laptop contract.

## Canonical Entry

Read:
1. `AGENTS.md`;
2. constitution;
3. `CURRENT_STATE.md`;
4. `PROGRAM_ROADMAP.md`;
5. `CODE_MODEL_SUPREMACY_STRATEGY.md`;
6. `docs/canonical/MSTR_FRONTIER_ACCELERATION_STRATEGY.md`;
7. MSTR-000A package;
8. this full MSTR-000B package, including `research-frontier-2026-09-04.md`.

Live GitHub truth and the exact active task remain authoritative. The frontier strategy is a planning constraint, not an external-effect authority.

## First Implementation Unit

Start with B001/B002 unless live repository truth shows a canonical successor already implemented.

Do not modify or close active PR #38 work unless its exact task and branch ownership require it. MSTR-000B planning was authored while PR #38 (`A003 event log + deterministic replay`) was open.

The historical first-unit language above is preserved as planning history. On resume, always use live task-gate truth rather than restarting completed tasks.

## Key Design Decisions

- `MSTR-000B` does not select a final model.
- `Mellum-4b-base` is mandatory to review but receives no admission privilege.
- B013 must bind a frontier-freshness snapshot before stable-pool freeze; a materially plausible post-scan challenger cannot be ignored because it arrived late.
- `K2 Horizon 3.7B` is a mandatory 2026-09-04 frontier-review input only; it is not admitted or authorized by this handoff.
- if B013 finds a plausible challenger without equivalent evidence, B013 stays `PENDING` and a separately governed refresh/task-graph amendment is required; completed B005-B012 history is not rewritten.
- new candidate weight access is separately gated;
- student self-alignment is first-class;
- teacher output is untrusted until independently verified;
- difficulty is student/checkpoint-relative;
- verifier health is part of training admission;
- test generation is a core learned skill;
- feature/greenfield tasks are first-class;
- Q4 regression is part of every material training promotion and remains the mandatory quality/product anchor;
- future Q3/Q2/structured-ternary or Sherry-class work is additive research and cannot replace Q4 promotion;
- when upstream exposes compatible base/intermediate/final checkpoints, downstream training planning should compare checkpoint lineage rather than assume the final assistant checkpoint is the best parent;
- MSTR-003 downstream planning must include production-compatible executable RL, dynamic synthetic environments, previous-MSTR bootstrap with independent admission, targeted trajectory-feedback research, and reward-shortcut resistance;
- MSTR-004 downstream planning should optimize TTVC through low-bit/runtime/kernel/speculation/parallel-generation and FAST/NORMAL/DEEP effort-control tournaments under exact hardware identity;
- MSTR-006 headline evaluation must be sealed against future Git history, public-solution/network leakage, cached solutions, and evaluator extraction;
- default runtime remains one builder + independent deterministic verifier;
- WePLD is the primary Half Moon full-system partner, not a raw-model score multiplier.

## Execution Discipline

For every B-task:

```text
verify live main
-> task eligibility
-> focused branch
-> smallest compliant implementation
-> tests/fixtures/evidence
-> frozen quality gates
-> PR
-> exact-head review
-> resolve material findings
-> merge expected head
-> canonical reconciliation
-> next eligible task
```

For B013 specifically:

```text
exact B013 entry
-> frontier snapshot
-> explicit disposition of material post-scan releases
-> if unresolved plausible challenger exists: STOP / preserve B013=PENDING
-> create separately governed refresh amendment
-> resume only after canonical equivalent qualification or evidence-backed rejection
-> freeze stable pool
```

No force-push/rebase/destructive history rewrite.

## Hard Stop

If a task requires:
- new model weight access outside current authority;
- paid compute/API;
- large corpus ingestion;
- weight-changing training;
- private user trace ingestion;

stop at the exact gate with a complete authorization envelope. Do not infer permission from generic continuation.

Also stop candidate-pool freeze when the B013 frontier snapshot exposes a materially plausible challenger whose missing evidence could change the pool decision. Do not convert a freshness problem into an evidence-free rejection.
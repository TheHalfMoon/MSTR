# MSTR-000 Implementation Handoff

## Mission
Implement MSTR-000 exactly from the canonical Spec Kit package. Do not redesign, select a backbone early, or start long training.

## Start Here
Read: `AGENTS.md`, constitution, current state, program roadmap, spec, clarification, research, plan, data model, contracts, quickstart, checklist, tasks.

## Starting State
```text
MSTR-000 = CANONICAL_ACTIVE
T000/T001/T002 = COMPLETE_CANONICAL
NEXT_EXECUTABLE_TASK_AFTER_PLAN = T003
FINAL_BACKBONE = UNSELECTED
MODEL_WEIGHT_ACCESS = NOT_AUTHORIZED_BY_T003
LONG_TRAINING = PROHIBITED
LARGE_SCALE_RL = PROHIBITED
```
Always verify live GitHub truth first.

## Strategy
For each task: verify base -> focused branch -> prerequisites/paths -> smallest implementation -> tests/evidence -> update checkbox only with evidence -> review/merge.

## Do Not
No force push/destructive history; no model weights unless task says so; no gated terms/paid API/rented compute without explicit authority; no large corpus/long SFT/RL; no one-benchmark selection; no silent hardware-floor change; no binaries/caches/credentials/private data in Git; no mandatory graph/vector/subagent complexity without tournament evidence.

## Product Invariant
`download -> install/launch -> open repo -> local coding assistance` without discrete GPU, provider login, API key, subscription, activation, mandatory cloud, or Docker/Python/Node for basic launch.

## Technical Split
MSTR-000 builds a Python qualification harness for manifests, rights, evidence, measurement, tournaments, interaction/context/verifier experiments. Do not prematurely build large desktop/distributed training systems.

## Done Means
Freeze measured support floor, distribution contract, local runtime/Q4, Interaction Contract v1, deterministic apply, minimal context, top backbone/top-two, environment MVP requirements, bounded MSTR-001 proposal, independent review, founder acceptance.

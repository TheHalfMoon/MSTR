name: Disabled malformed PR163 Qwen integrity builder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Historical malformed builder definition disabled; run 33961426755 is preserved as negative harness evidence."

name: B023 hardened binding repair exact-head qualification

on:
  push:
    branches:
      - evidence/b023-binding-repair-qualification-bd0a49c

permissions:
  contents: read

env:
  TARGET_SHA: bd0a49c9b6b00cc63dc60b148a059921de3c8d51
  TARGET_TREE: 0c0a17b79b6272cd1ef235a9875fd09b1cb44682
  BASE_SHA: db12d1467bb81185a9affffb4d470626ceceddfa

jobs:
  identity_scope:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}
          fetch-depth: 0
      - name: Verify exact candidate identity and immutable main
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TARGET_SHA"
          test "$(git rev-parse HEAD^{tree})" = "$TARGET_TREE"
          test "$(git rev-parse HEAD^)" = "$BASE_SHA"
          test "$(git merge-base "$BASE_SHA" "$TARGET_SHA")" = "$BASE_SHA"
          test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$BASE_SHA"
          test "$(git ls-remote origin refs/heads/fix/b023-cross-workstream-bindings-hardened-atomic-20260901 | cut -f1)" = "$TARGET_SHA"
      - name: Verify exact four-file scope
        shell: bash
        run: |
          set -euo pipefail
          expected="$(mktemp)"
          actual="$(mktemp)"
          cat >"$expected" <<'EOF'
          configs/task-gate/mstr-000b.json
          evidence/mstr-000b/B023-cross-workstream-binding-reconciliation.md
          src/mstr_qualify/task_gate.py
          tests/contract/test_task_gate.py
          EOF
          git diff --name-only "$BASE_SHA...$TARGET_SHA" | sort >"$actual"
          sort -o "$expected" "$expected"
          diff -u "$expected" "$actual"
          git diff --check "$BASE_SHA...$TARGET_SHA"
          ! grep -q '^\.github/workflows/' "$actual"
      - name: Verify hardened governance bindings and B023 pending state
        shell: bash
        run: |
          set -euo pipefail
          grep -F -- '- [ ] **B023 Implement verifier-health evaluator on controlled fixtures.**' specs/002-code-model-supremacy-foundation/tasks.md
          python - <<'PY'
          import json
          from pathlib import Path

          catalog = json.loads(Path('configs/task-gate/mstr-000b.json').read_text())
          b023 = catalog['tasks']['B023']
          assert b023['canonical_state'] == 'PENDING'
          assert set(b023['prerequisites']) == {'A006', 'A014', 'B002', 'B022'}
          expected_tasks = 'specs/001-agent-harness-verified-loop-foundation/tasks.md'
          expected = {
              'A006': 'evidence/mstr-000a/A006-finalizer.md',
              'A014': 'evidence/mstr-000a/A014-verifier-shortcuts.md',
          }
          external = catalog['external_prerequisites']
          for task_id, evidence_path in expected.items():
              binding = external[task_id]
              assert binding['workstream_id'] == 'MSTR-000A'
              assert binding['task_identity'] == f'MSTR-000A / {task_id}'
              assert binding['tasks_file'] == expected_tasks
              assert binding['state_evidence'] == evidence_path
              assert evidence_path in binding['evidence_outputs']
              assert binding['required_state'] == 'COMPLETE_CANONICAL'
          PY

  quality:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install project and development gates
        run: python -m pip install -e '.[dev]'
      - name: Focused task-gate tests
        run: python -m pytest tests/contract/test_task_gate.py tests/integration/test_task_gate_cli.py -q
      - name: Offline schema validation
        run: python -m mstr_qualify validate
      - name: Full pytest
        run: python -m pytest -q
      - name: Ruff
        run: python -m ruff check .
      - name: Strict mypy
        run: python -m mypy src

  complete:
    runs-on: ubuntu-24.04
    needs:
      - identity_scope
      - quality
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}
          fetch-depth: 0
      - name: Recheck exact candidate immutability
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TARGET_SHA"
          test "$(git rev-parse HEAD^{tree})" = "$TARGET_TREE"
          test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$BASE_SHA"
          test "$(git ls-remote origin refs/heads/fix/b023-cross-workstream-bindings-hardened-atomic-20260901 | cut -f1)" = "$TARGET_SHA"

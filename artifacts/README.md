# Qualification Artifacts

Small, reviewable JSON/JSONL manifests, decisions, and result records belong here.
Large model weights, datasets, caches, raw logs, and other binaries are external and referenced
by immutable identity. T003 creates only the directory contract.

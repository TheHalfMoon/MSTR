# Qualification Configurations

Configuration namespaces created by T003. Later tasks populate versioned records under:

- `hardware/`
- `candidates/`
- `runtimes/`
- `interaction/`
- `context/`

T003 intentionally selects no model, runtime, interaction contract, or context backend.
